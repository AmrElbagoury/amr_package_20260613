[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "amr_package_20260613"   # must be unique on PyPI
version = "0.1.5"
description = "A test package"