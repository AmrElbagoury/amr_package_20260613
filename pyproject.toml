[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "my-package-yourname"   # must be unique on PyPI
version = "0.1.0"
description = "A test package"